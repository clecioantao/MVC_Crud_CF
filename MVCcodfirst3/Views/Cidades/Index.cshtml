@model IEnumerable<MVCcodfirst3.Models.Cidade>

@{
    ViewBag.Title = "Cidades";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Adicionar", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NomeCidade)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NomeCidade)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.CidadeId }) |
            @Html.ActionLink("Detalhes", "Details", new { id=item.CidadeId }) |
            @Html.ActionLink("Deletar", "Delete", new { id=item.CidadeId })
        </td>
    </tr>
}

</table>
